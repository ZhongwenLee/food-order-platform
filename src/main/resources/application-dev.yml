spring:
  datasource:
    url: jdbc:mysql://localhost:3306/food_order_platform?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 5s

logging:
  level:
    root: info
    com.foodorder: debug
    org.springframework.web: info
    org.springframework.security: info